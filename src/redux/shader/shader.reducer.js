export default function shaderReducer(state = initialState, action) {
  switch (action.type) {
  }

  return state;
}

const initialState = {
  loaded: false,
  loading: false,
  vertSource: '',
  fragSource: '',
};
