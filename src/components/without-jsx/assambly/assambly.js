import React from 'react';
import PropTypes from 'prop-types';

const Assambly = ({ memory }) => {
  return null;
};

Assambly.propTypes = {
  memory: PropTypes.arrayOf(PropTypes.number).isRequired,
};

export default Assambly;
